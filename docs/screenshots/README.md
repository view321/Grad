# Screenshots

The images [`README.md`](../../README.md) expects, by filename. Drop a PNG in
with the matching name and it appears; nothing else has to change.

| File | Shot |
|---|---|
| `workspace.png` | the hero: several windows tiled at once — chat, tasks, funnel |
| `ledger.png` | the ledger window: expectations against outcomes |
| `funnel.png` | the funnel window: 300 → 50 → 15, with survivors in rank order |
| `quota.png` | the quota window: tokens and credits by stage |

Not captured yet, and worth adding when there is something real in it:
`notebook.png` (the notebook window — embedded JupyterLab with the verify
banner). Add the file, then a cell for it in the README's table.

Take them at a wide viewport (the workspace is a tiling shell and looks cramped
below ~1400px) and check what is legible before committing: window titles carry
project names, the chat carries whatever was being discussed, and paths under
`C:\Users\<you>\` are readable at full size.
